# Dogrudan bagimliliklar - tam surume sabitlendi.
# Diger paketler (pydantic, starlette, torch, transformers ...) bunlarin
# bagimliligi olarak otomatik kurulur, ayrica yazmaya gerek yok.
fastapi==0.141.1
uvicorn==0.53.0

# Adim 5: kod parcalarini ve sorgulari sayisal vektore cevirir.
# torch'u de beraberinde getirir; ilk kurulum buyuktur.
sentence-transformers==6.0.1

# Adim 6: vektorleri saklar ve "en yakin 5 vektor" aramasini yapar.
# Yerel modda calisir (Docker gerekmez); Adim 22'de sunucu moduna gecilecek.
qdrant-client==1.19.1

# Adim 8: LLM cevabi. Google Gen AI SDK (Gemini).
google-genai==2.23.0

# .env dosyasindaki API anahtarini okur.
python-dotenv==1.2.3
